# Local lab only. Copy to .env and choose a password with at least 8 characters.
NEO4J_PASSWORD=cpg-password
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
MONGO_INITDB_DATABASE=cpg
CONNECT_URL=http://localhost:8083
