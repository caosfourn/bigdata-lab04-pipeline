# ─────────────────────────────────────────────────────────────────────
# Lab 04 - Spark Streaming: CPG Pipeline
# ─────────────────────────────────────────────────────────────────────

# ── Thành viên 1: Parser Service (KHÔNG cần gói ngoài, dùng stdlib) ──
# ast, hashlib, os, json — đều có sẵn trong Python 3.10+

# ── Thành viên 2: Kafka Integration ──────────────────────────────────
kafka-python==2.0.2          # Kafka Producer/Consumer client

# ── Thành viên 3: MongoDB + Spark ────────────────────────────────────
pyspark==3.5.1               # Apache Spark (bao gồm Structured Streaming)
pymongo==4.6.3               # MongoDB Python driver

# ── Notebook & Reporting ──────────────────────────────────────────────
jupyter==1.0.0               # Jupyter Notebook
jupyter-book==0.15.1         # Jupyter Book (cho submission)
ipykernel==6.29.5            # IPython kernel

# ── Dev Tools ─────────────────────────────────────────────────────────
python-dotenv==1.0.1         # Quản lý env variables (.env file)
